# Keyless smoke test composition (no API key, no model).
#
# Run from this directory with the vendored Cordis bin, pointing tsx at the
# runtime tsconfig so bare @deepseek-ai/* names resolve (see README):
#
#   $env:TSX_TSCONFIG_PATH = 'C:\D\Code\dsh-vision-without-vision-model\tsconfig.runtime.json'
#   node --import tsx C:/D/Code/deepseek-harness/vendor/cordis/bin.js
#
# Relative specifiers resolve against this directory.
- name: '@deepseek-ai/dsh-system-prompt'
- name: '@deepseek-ai/dsh-tools'
- name: '../src/index.ts'
- name: './driver.ts'
